export const metadata = {
  title: 'Virtual Coach Training | Transformational Sports',
  alternates: { canonical: '/virtual-coach-training' },
};

export default function VirtualCoachTrainingLayout({ children }) {
  return children;
}
